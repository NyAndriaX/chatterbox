import React from "react";
import { Box } from "@mui/material";

const ComponentMessage = () => {
	return <Box sx={{ height: "80vh", border: "dotted" }}>ComponentMessage</Box>;
};

export default ComponentMessage;
